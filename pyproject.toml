[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Literary World Map Flask Application"
requires-python = ">=3.11"
dependencies = [
    "folium>=0.19.5",
    "pandas>=2.2.3",
    "flask>=2.3.3",
]
